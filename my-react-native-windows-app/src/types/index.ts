export type RootStackParamList = {
  Home: undefined;
  // Add other screens here as needed
};

export interface User {
  id: string;
  name: string;
  email: string;
  // Add other user properties here as needed
}

// Add other types and interfaces as needed for your application